@model FINALPROJECT.ViewModels.Admin.AdminDashboardViewModel
@{
    ViewData["Title"] = "Dashboard Overview";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="d-flex align-items-center justify-content-between mb-4">
    <div>
        <h1 class="admin-page-title mb-0">Dashboard Overview</h1>
        <p class="text-muted small">Welcome back, here's what's happening with Flavorly today.</p>
    </div>
    <div class="d-flex gap-2">
        <a asp-controller="Recipe" asp-action="Create" class="btn text-white rounded-pill px-3" style="background-color: var(--admin-primary); border-color: var(--admin-primary);">+ Add Recipe</a>
    </div>
</div>

<!-- Stats Overview -->
<div class="row g-4 mb-4">
    <div class="col-md-3">
        <div class="stat-card">
            <div class="d-flex align-items-start justify-content-between mb-2">
                <div class="d-flex align-items-center justify-content-center rounded text-white" style="width: 40px; height: 40px; background-color: rgba(221, 110, 53, 0.1);">
                    <i class="bi bi-egg-fried" style="color: var(--admin-primary);"></i>
                </div>
                <span class="badge bg-secondary-subtle text-secondary rounded-pill">Total</span>
            </div>
            <h2 class="fw-bold mb-0">@Model.TotalRecipes.ToString("N0")</h2>
            <div class="text-muted small">Total Recipes</div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="stat-card">
            <div class="d-flex align-items-start justify-content-between mb-2">
                <div class="d-flex align-items-center justify-content-center rounded" style="width: 40px; height: 40px; background-color: rgba(66, 133, 244, 0.1);">
                    <i class="bi bi-people" style="color: #4285f4;"></i>
                </div>
                <span class="badge bg-secondary-subtle text-secondary rounded-pill">Total</span>
            </div>
            <h2 class="fw-bold mb-0">@Model.TotalUsers.ToString("N0")</h2>
            <div class="text-muted small">Total Users</div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="stat-card">
            <div class="d-flex align-items-start justify-content-between mb-2">
                <div class="d-flex align-items-center justify-content-center rounded" style="width: 40px; height: 40px; background-color: rgba(161, 66, 244, 0.1);">
                    <i class="bi bi-play-btn" style="color: #a142f4;"></i>
                </div>
                <span class="badge bg-secondary-subtle text-secondary rounded-pill">Stable</span>
            </div>
            <h2 class="fw-bold mb-0">@Model.TotalVideos.ToString("N0")</h2>
            <div class="text-muted small">Total Videos</div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="stat-card">
            <div class="d-flex align-items-start justify-content-between mb-2">
                <div class="d-flex align-items-center justify-content-center rounded" style="width: 40px; height: 40px; background-color: rgba(234, 67, 53, 0.1);">
                    <i class="bi bi-exclamation-circle" style="color: #ea4335;"></i>
                </div>
                @if (Model.PendingReportsCount > 0) {
                    <span class="badge bg-danger-subtle text-danger rounded-pill">@Model.PendingReportsCount Pending</span>
                } else {
                    <span class="badge bg-success-subtle text-success rounded-pill">Clear</span>
                }
            </div>
            <h2 class="fw-bold mb-0">@Model.PendingReportsCount</h2>
            <div class="text-muted small">Pending Reports</div>
        </div>
    </div>
</div>

<div class="row g-4 mb-4">
    <!-- Chart -->
    <div class="col-md-8">
        <div class="stat-card h-100">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h6 class="fw-bold mb-0">Growth Analytics</h6>
                <div class="d-flex gap-3 small">
                    <div class="d-flex align-items-center gap-1"><i class="bi bi-circle-fill text-dark" style="font-size: 8px;"></i> New Users</div>
                    <div class="d-flex align-items-center gap-1"><i class="bi bi-circle-fill" style="color: var(--admin-primary); font-size: 8px;"></i> New Recipes</div>
                </div>
            </div>
            <canvas id="growthChart" height="250"></canvas>
        </div>
    </div>
    
    <!-- Activity -->
    <div class="col-md-4">
        <div class="stat-card h-100 d-flex flex-column">
            <h6 class="fw-bold mb-4">Recent Activity</h6>
            <div class="flex-grow-1">
                @foreach (var u in Model.RecentUsers)
                {
                    var timeAgo = (DateTime.UtcNow - u.CreatedAt);
                    var agoStr = timeAgo.TotalMinutes < 60 ? $"{(int)timeAgo.TotalMinutes} min ago"
                               : timeAgo.TotalHours < 24 ? $"{(int)timeAgo.TotalHours}h ago"
                               : $"{(int)timeAgo.TotalDays}d ago";
                    <div class="d-flex align-items-start gap-3 mb-3">
                        <div class="rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px; background-color: rgba(221, 110, 53, 0.1);">
                            <i class="bi bi-person-plus" style="color: var(--admin-primary); font-size: 0.8rem;"></i>
                        </div>
                        <div>
                            <div class="small fw-semibold">New signup: @u.DisplayName</div>
                            <div class="text-muted" style="font-size: 0.7rem;">@agoStr.ToUpper()</div>
                        </div>
                    </div>
                }
                @foreach (var r in Model.RecentRecipeActivity)
                {
                    var timeAgo = (DateTime.UtcNow - r.CreatedAt);
                    var agoStr = timeAgo.TotalMinutes < 60 ? $"{(int)timeAgo.TotalMinutes} min ago"
                               : timeAgo.TotalHours < 24 ? $"{(int)timeAgo.TotalHours}h ago"
                               : $"{(int)timeAgo.TotalDays}d ago";
                    <div class="d-flex align-items-start gap-3 mb-3">
                        <div class="rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px; background-color: rgba(66, 133, 244, 0.1);">
                            <i class="bi bi-egg-fried" style="color: #4285f4; font-size: 0.8rem;"></i>
                        </div>
                        <div>
                            <div class="small fw-semibold">New recipe: @r.Title</div>
                            <div class="text-muted" style="font-size: 0.7rem;">@agoStr.ToUpper()</div>
                        </div>
                    </div>
                }
                @if (!Model.RecentUsers.Any() && !Model.RecentRecipeActivity.Any())
                {
                    <div class="text-center text-muted py-4">
                        <i class="bi bi-clock-history fs-3 d-block mb-2"></i>
                        <div class="small">No recent activity yet.</div>
                    </div>
                }
            </div>
            <a asp-controller="Admin" asp-action="Users" class="btn btn-outline-secondary w-100 rounded-pill mt-3">View All Activity</a>
        </div>
    </div>
</div>

<!-- Most Liked Recipes (Top 5 Recent for now) -->
<div class="stat-card p-0 overflow-hidden">
    <div class="p-4 border-bottom d-flex align-items-center justify-content-between">
        <h6 class="mb-0 fw-bold">Recent Recipes</h6>
        <button class="btn btn-sm btn-light border rounded-pill">Export CSV</button>
    </div>
    <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
            <thead class="table-light text-muted small text-uppercase">
                <tr>
                    <th class="ps-4" style="font-weight: 600;">Thumbnail</th>
                    <th style="font-weight: 600;">Recipe Title</th>
                    <th style="font-weight: 600;">Author</th>
                    <th style="font-weight: 600;">Likes</th>
                    <th class="text-end pe-4" style="font-weight: 600;">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var r in Model.RecentRecipes)
                {
                    <tr>
                        <td class="ps-4">
                            <img src="@r.HeroImageUrl" class="rounded" width="48" height="40" style="object-fit:cover" />
                        </td>
                        <td class="fw-semibold">@r.Title</td>
                        <td>
                            <div class="d-flex align-items-center gap-2">
                                <img src="/files/default-avatar.png" class="rounded-circle" width="24" height="24" />
                                <span class="small">@(r.Author?.DisplayName ?? "Unknown")</span>
                            </div>
                        </td>
                        <td>
                            <span class="small" style="color: var(--admin-primary); font-weight: 600;">-</span>
                        </td>
                        <td class="text-end pe-4">
                            <button class="btn btn-sm btn-link text-muted"><i class="bi bi-three-dots"></i></button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var chartLabels = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.ChartLabels));
            var newUsersData = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.NewUsersPerDay));
            var newRecipesData = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.NewRecipesPerDay));

            var ctx = document.getElementById('growthChart').getContext('2d');
            var growthChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: chartLabels,
                    datasets: [
                        {
                            label: 'New Users',
                            data: newUsersData,
                            borderColor: '#2c2520',
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            tension: 0.4,
                            pointRadius: 3,
                            pointHoverRadius: 5
                        },
                        {
                            label: 'New Recipes',
                            data: newRecipesData,
                            borderColor: '#dd6e35',
                            backgroundColor: 'transparent',
                            borderWidth: 2,
                            tension: 0.4,
                            pointRadius: 3,
                            pointHoverRadius: 5
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { display: false }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            grid: {
                                drawBorder: false
                            },
                            ticks: {
                                stepSize: 1
                            }
                        },
                        x: {
                            grid: {
                                display: false,
                                drawBorder: false
                            }
                        }
                    }
                }
            });
        });
    </script>
}
